import { createSlice } from "@reduxjs/toolkit";

export const userName = createSlice({
    name: 'userName',
    initialState: 'Jose Luiiiiiiiiis',
    reducers: {

    }
})

export default userName.reducer